<%= form_with(model: object_book, local: true) do |form| %>
  <% if object_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(object_book.errors.count, "error") %> prohibited this object_book from being saved:</h2>

      <ul>
      <% object_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
